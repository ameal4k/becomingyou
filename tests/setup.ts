// tests/setup.ts
import "@testing-library/jest-dom/vitest";



// (Optional) quiet console noise during tests:
// const originalError = console.error;
// beforeAll(() => { console.error = (...args) => { /* filter if needed */ }; });
// afterAll(() => { console.error = originalError; });
